#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int> nums = {1,2,4,5,6,8,13,24,49,49,3};
    sort(nums.begin(), nums.end());

    int ret = 0;
    int l = 0;
    int r = nums.size()-1;

    while(l < r){
        int mid = l + (r - l)/2;

        
    }
    cout<<ret;
}